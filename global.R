# ------------------------------------------------------------------------------
# Some code that only ever needs to run once, eg loading packages, pre-processing data

# Packages
library(ggplot2)
library(ggTag)
library(shiny)
library(shinythemes)

# Graph styling
theme_set(theme_bw(base_size = 14))

# Could do some other pre-processing here...